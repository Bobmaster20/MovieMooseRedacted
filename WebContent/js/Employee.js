// A $( document ).ready() block.
$( document ).ready(function() {
    function mailingTable(mailingList){
    	var table_string = "<tr>d style=\"text-align:center\">";
    	for(mailing in mailingList){
    		//table_string += mailing.
    		console.log(mailing);
    		
    	}
    }
});